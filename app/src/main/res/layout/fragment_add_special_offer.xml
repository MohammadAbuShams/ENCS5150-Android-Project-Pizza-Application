<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add Special Offer"
        android:textSize="18sp"
        android:textColor="@android:color/black"
        android:gravity="center"
        android:paddingBottom="16dp"
        android:background="@color/pizza_theme_color"/>

    <Spinner
        android:id="@+id/spinner_offer_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:entries="@array/offer_names" />

    <Spinner
        android:id="@+id/spinner_pizza_type"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:entries="@array/pizza_categories"/>

    <Spinner
        android:id="@+id/spinner_pizza_size"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:entries="@array/pizza_sizes"
        android:layout_marginTop="16dp"/>

    <Spinner
        android:id="@+id/spinner_offer_period"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:entries="@array/offer_period"
        android:layout_marginTop="16dp"/>

    <EditText
        android:id="@+id/editText_total_price"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Total Price"
        android:padding="8dp"
        android:inputType="numberDecimal"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/button_save_offer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save Offer"
        android:layout_marginTop="24dp"
        android:background="@drawable/green_button"/>
</LinearLayout>
